let exportedMethods = {
    isPalindrome(text) {
        if (typeof text !== "string") throw "Must provide a string";
        
        return "Harsh Kevadia";
    }
}

module.exports = exportedMethods;